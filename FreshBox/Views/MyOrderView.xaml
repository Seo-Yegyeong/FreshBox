<UserControl x:Class="FreshBox.Views.MyOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:FreshBox.ViewModels"
             xmlns:local="clr-namespace:FreshBox.Views"
             d:DataContext="{d:DesignInstance Type=viewModels:MyOrderViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#fff">
    <Grid Margin="10">
        <StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" Margin="0 0 0 10" >
                <TextBlock Text="상품명:" VerticalAlignment="Center" Margin="10" FontFamily="Noto Sans KR" FontWeight="bold" FontSize="14"/>
                <TextBox Width="200" Height="30" Text="{Binding InputProductName, Mode=TwoWay}" Margin=" 20 10 10 10" Background="#fefefe"/>
                <TextBlock Text="수량:" VerticalAlignment="Center" Margin="10" FontFamily="Noto Sans KR" FontWeight="bold" FontSize="14"/>
                <TextBox Width="200" Text="{Binding InputQuantity, Mode=TwoWay}" Margin="20 10 10 10" Background="#fefefe"/>
                <Button x:Name="입고" Content="입고" Background="#9EDE55" MinWidth="100" Margin="20, 10, 10, 10" Command="{Binding InboundMyOrderCommand}"/>
                <Button Content="Reload Data" Command="{Binding LoadMyOrdersCommand}" Margin="20, 10, 10, 10" />
            </StackPanel>

            <StackPanel>
                <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding MyOrders}" 
                    SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                    Height="300" Margin="0 0 0 10">
                    <!--
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                        <DataGridTextColumn Header="Order Date" Binding="{Binding InputOrderDate}" />
                        <DataGridTextColumn Header="Product ID" Binding="{Binding InputProductId}" />
                        <DataGridTextColumn Header="Product Name" Binding="{Binding InputProductName}" />
                        <DataGridTextColumn Header="Quantity" Binding="{Binding InputQuantity}" />
                    </DataGrid.Columns>
                    -->
                </DataGrid>
            </StackPanel>
            

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                
                <Button Content="Add" Command="{Binding AddMyOrderCommand}" Margin="10"/>
                <!--
                <Button Content="Update" Command="{Binding UpdateMyOrderCommand}" />
                <Button Content="Delete" Command="{Binding DeleteMyOrderCommand}" />
                -->
             </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
