<UserControl x:Class="FreshBox.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:FreshBox.Views"
             xmlns:viewmodels="clr-namespace:FreshBox.ViewModels" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <viewmodels:CategoryViewModel x:Key="CategoryVM" />
        <viewmodels:ProductViewModel x:Key="ProductVM" />
    </UserControl.Resources>
    <Grid>
        <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Margin="103,95,497,0" Width="200"/>
        <ComboBox
            ItemsSource="{Binding Categories}"
            DisplayMemberPath="CategoryName"
            SelectedValuePath="ID"
            SelectedValue="{Binding SelectedCategoryId}"
            Width="200"
            Height="50" Margin="103,145,497,255"/>
    </Grid>
</UserControl>
